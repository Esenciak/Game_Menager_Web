@{
    ViewData["Title"] = "Home Page";
}
<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Game Menager - D&D</title>
    <style>
  /* Style wyglądu strony */
  body {
      margin: 0;
      padding: 0;
      font-family: 'Gothic', sans-serif;
      background-color: #202020;
      color: #fff;
      justify-content: center;
      align-items: center;
      height: 1080px;
  }

  .container {
      width: 400px;
      padding: 40px;
            background-color: #1A1A1A;
      border-radius: 10px;
            box-shadow: 0 0 10px #1A1A1A;
      text-align: center;
  }

  h1 {
      font-size: 2em;
      color: #c0c0c0;
      margin-bottom: 20px;
  }
/* Styl krótkiej opowieści */
  .intro {
      font-size: 1em;
      margin-bottom: 30px;
      color: #a9a9a9;
  }
 /* Style dla guzików */
  .login-button {
      padding: 10px 20px;
      background-color: #444;
      border: none;
      border-radius: 5px;
      color: white;
      font-size: 1em;
      cursor: pointer;
      transition: background-color 0.3s;
  }

  .login-button:hover {
      background-color: #666;
  }

    </style>
</head>
<body>
    <div>
    @* <div class="container"> *@
        <h1>Game Menager - D&D</h1>
        <p class="intro">
            W mrocznym świecie, gdzie cienie kryją sekrety, a odwaga jest kluczem do przetrwania,
            wyrusz na niezwykłą podróż pełną niebezpieczeństw i odkryć...
        </p>

          @*   <a asp-controller="Loggin" asp-action="Index" class="btn btn-primary mx-2  login-button ">

                Zaloguj się
            </a> *@
    </div>
</body>
</html>
